<!DOCTYPE html>
<html lang="en">
  <head>
    {% include head.liquid %}
  </head>
  <body>
    <header class="header">
      {% include navbar.liquid %}
    </header>
    <main>
      {%- if cover -%}
        {% include cover.liquid %}
      {%- endif -%}
      <article>
        {{ content }}
      </article>
    </main>
    <footer>
      {% include footer.liquid %}
    </footer>
    {% include scripts.liquid %}
  </body>
</html>
