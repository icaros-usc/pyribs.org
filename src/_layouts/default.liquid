<!DOCTYPE html>
<html lang="en">
  <head>
    {% include head.liquid %}
  </head>
  <body>
    <header class="header">
      {% include navbar.liquid %}
    </header>
    <main>
      <h1 class="font-light text-light text-4xl">{{ title }}</h1>
      <article>
        {{ content }}
      </article>
    </main>
    <footer>
      {% include footer.liquid %}
    </footer>
    {% include scripts.liquid %}
  </body>
</html>
